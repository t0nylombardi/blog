---
import BaseWrapper from '../layouts/BaseWrapper.astro'
import BlogPostList from '../components/BlogPostList.astro'
import {ContactForm} from '../components/ContactForm/'
import MySocials from '../components/MySocials.tsx'
import CTA from '../components/UI/CTA.astro'
import AgeCounter from '../components/AgeCounter'
import {AboutSection, ProjectSection} from '../components/Sections'
---

<style>
  section:last-of-type {
    margin-top: 2rem;
    margin-bottom: 16rem;
  }

  .icon svg {
    width: 1rem;
    height: 1rem;
  }

  .showcase {
    max-width: calc(4 * 4rem + 3 * 4rem);
  }

  .showcase svg {
    width: 4rem;
    height: 4rem;
  }

  .hover {
    @apply hover:text-gray-200;
  }

  .text-gradient {
    background: -webkit-linear-gradient(90deg, #a478e8 0%, #3b82f6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
</style>

<BaseWrapper>
  <div
    class="mx-auto bg-red-300 px-6 md:px-8 my-2 md:my-2 py-4 lg:py-40 scroll-snap-y scroll-smooth h-screen overflow-auto hide-scrollbar"
  >
    <section id="hello" class="container mx-auto px-4 sm:px-8 py-16 flex flex-col-reverse md:flex-row items-center">
      <!-- Text Section -->
      <div class="w-full md:w-1/2 md:text-left">
        <div class="text-center md:text-left">
          <p class="text-base sm:text-2xl font-medium">Hey, I'm</p>
          <h1 class="text-3xl sm:text-3xl font-bold mt-2">Anthony Lombardi</h1>
          <h2 class="text-gradient text-2xl mt-1">&gt; Full-Stack Engineer</h2>
        </div>

        <div class="font-medium mt-6 space-y-1">
          <p class="comment">// personal data</p>

          <p>
            <span class="text-[#4D5BCE]">let </span>
            <span class="text-[#43D9AD]">age</span> =
            <span id="age" class="text-[#E99287] block pl-6 md:inline md:pl-0">
              <AgeCounter client:only="react" /></span
            >
          </p>

          <p>
            <span class="text-[#4D5BCE]">let </span>
            <span class="text-[#43D9AD]">location</span> =
            <span class="text-[#E99287] block pl-6 md:inline md:pl-0">"Westchester, New York"</span>
          </p>

          <MySocials />
        </div>

        <div class="mt-6">
          <CTA link="resume" />
        </div>
      </div>

      <!-- Avatar Section -->
      <div class="w-full md:w-1/2 flex justify-center mb-8 md:mb-0">
        <img src="/amoji_avatar.svg" alt="Avatar" class="max-w-[200px] sm:max-w-xs md:max-w-sm lg:max-w-md" />
      </div>
    </section>

    <AboutSection client:only="react" />

    <ProjectSection client:load />

    <section id="blog" class="scroll-snap-start h-screen flex flex-col items-center justify-center my-[12rem]">
      <h3 class="text-4xl font-medium">_blog</h3>
      <BlogPostList limit={3} />
      <a href="/blog" class="btn-primary text-2xl block py-16">See more blog posts</a>
    </section>

    <!-- <ContactForm client:only="react" /> -->
  </div>
</BaseWrapper>
